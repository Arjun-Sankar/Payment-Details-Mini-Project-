import { UsercardDirective } from './usercard.directive';

describe('UsercardDirective', () => {
  it('should create an instance', () => {
    const directive = new UsercardDirective();
    expect(directive).toBeTruthy();
  });
});
