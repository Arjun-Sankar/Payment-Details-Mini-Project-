import { StartsupperPipe } from './startsupper.pipe';

describe('StartsupperPipe', () => {
  it('create an instance', () => {
    const pipe = new StartsupperPipe();
    expect(pipe).toBeTruthy();
  });
});
